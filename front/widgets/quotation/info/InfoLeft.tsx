import { Box } from '@mui/material'

export const InfoLeft = (): React.ReactNode => {
  return (
    <Box
      sx={{
        all: 'unset',
        display: 'flex',
        alignItems: 'center',
        width: '100px',
        maxWidth: '100px',
        height: '18px',
      }}
    ></Box>
  )
}
