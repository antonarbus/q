# UI Improvement TODOs

- [ ] **Bottom "+" discoverability** — consider a dashed-border drop zone instead of a bare icon so it reads as an intentional add target
- [ ] When open quotation list directly nav bar is hidden, it is a bug
